services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=
    restart: unless-stopped
    depends_on:
      - server
    # No ports exposed to host as requested
    # expose:
    #   - "80"

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=8080
      # Configure server-side settings if needed
    # No ports exposed to host as requested
    # expose:
    #   - "8080"
