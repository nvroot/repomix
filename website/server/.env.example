# Server Configuration
PORT=8080
NODE_ENV=production

# Bundled Mode Settings (used in Dockerfile)
# REPOMIX_WORKER_PATH=/app/dist-bundled/worker.mjs
# REPOMIX_WASM_DIR=/app/dist-bundled/wasm

# Google Cloud Configuration
# Optional: Used for distributed tracing in Cloud Run
GOOGLE_CLOUD_PROJECT=your-project-id
# GCP_PROJECT=your-project-id

# Warmup Mode
# Set to 'true' to run in warmup mode for compile cache generation
WARMUP_MODE=false
